// src/pages/ProfileReviews.tsx

import { Typography } from '@mui/material';

export const ProfileReview = () => {
  return (
    <Typography variant="body2">
      Select a profile from the moderation list to review details.
    </Typography>
  );
};