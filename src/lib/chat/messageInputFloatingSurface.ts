/**
 * Shared MessageInput props for in-page popover and `/chat-popup` surfaces,
 * where the narrow-viewport ⋯ control is redundant with the expand chevron.
 */
export const MESSAGE_INPUT_FLOATING_SURFACE = {
  hideNarrowMoreOptions: true,
} as const;
