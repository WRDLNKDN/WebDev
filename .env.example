# ======================================================
# SUPABASE — FRONTEND (SAFE TO USE IN BROWSER)
# ======================================================

# Local Supabase API URL (from `npx supabase start`)
VITE_SUPABASE_URL=http://127.0.0.1:54321

# Public anon key (sb_publishable_*)
VITE_SUPABASE_ANON_KEY=sb_publishable_REPLACE_ME

# ======================================================
# SUPABASE — BACKEND / ADMIN ONLY (NEVER IN FRONTEND)
# ======================================================

# Service role key (sb_secret_*) used ONLY server-side
SUPABASE_SERVICE_ROLE_KEY=sb_secret_REPLACE_ME

# ======================================================
# OAUTH / OIDC PROVIDERS (USED BY supabase/config.toml via env(...))
# Fill in ONLY the providers you enable.
# ======================================================

# --- Google ---
SUPABASE_GOOGLE_CLIENT_ID=
SUPABASE_GOOGLE_CLIENT_SECRET=

# --- GitHub ---
SUPABASE_GITHUB_CLIENT_ID=
SUPABASE_GITHUB_CLIENT_SECRET=

# --- Facebook (Meta) ---
SUPABASE_FACEBOOK_CLIENT_ID=
SUPABASE_FACEBOOK_CLIENT_SECRET=

# --- Microsoft / Azure AD (OIDC) ---
# Required for Microsoft sign-in. Create app at portal.azure.com, add redirect URI:
#   http://localhost:54321/auth/v1/callback
# Then run: supabase stop && supabase start
SUPABASE_AZURE_CLIENT_ID=
SUPABASE_AZURE_CLIENT_SECRET=

# --- LinkedIn (OIDC) ---
SUPABASE_LINKEDIN_CLIENT_ID=
SUPABASE_LINKEDIN_CLIENT_SECRET=

# ======================================================
# API (production)
# ======================================================
# Backend origin only — do NOT add /api at the end. The app appends /api/feeds etc.
# Leave empty in dev (Vite proxies /api to localhost:3001). In prod, set to your
# API server so the app does not request /api/feeds from the SPA host (which would
# return HTML and cause "Unexpected token '<'" when parsing JSON).
# Example: https://api.wrdlnkdn.com  (not https://api.wrdlnkdn.com/api)
VITE_API_URL=

# ======================================================
# ECWID STOREFRONT (embed under /store)
# ======================================================
# Ecwid store ID from your Ecwid dashboard. Leave empty to show "Store not configured."
VITE_ECWID_STORE_ID=

# Optional. Navbar Store link. If set, overrides the default https://wrdlnkdn.com/store-1.
VITE_STORE_URL=

# ======================================================
# OPTIONAL APP FLAGS
# ======================================================
VITE_ENABLE_ANALYTICS=false
